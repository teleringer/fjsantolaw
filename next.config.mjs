/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If images are only local, leave images block out.
  // If you later load external images, we can add `images.remotePatterns`.
};

export default nextConfig;
